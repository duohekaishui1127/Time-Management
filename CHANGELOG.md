# v2 改动

- UI 绘制从 `app.js` 拆到 `src/ui/canvas-ui.js`。
- `app.js` 变成控制器/状态机，减少 UI 与业务耦合。
- 新增 `level-validator.js`，启动前校验关卡数据。
- 新增关卡依赖环检测、地点可达性检查。
- 新增 `finishBefore` 通用任务约束。
- 新增可解释的 `scoreBreakdown`。
- 突发事件修改截止时间后，结果页显示真实有效截止时间。
- 存档升级到 v2，并兼容迁移 v1。
- 存档增加最佳分数、尝试次数、广告提前解锁记录。
- 默认解锁数量只从 `config.js` 配置，不再重复定义。
- 广告 Platform 不再直接 require 全局配置，平台层更加独立。
- 增加异步广告点击 busy 锁，避免用户重复点击并发弹窗。
- 关卡列表增加分页能力，未来超过 10 关无需重写选择页。
- 已选任务槽宽度根据任务数量动态计算。
- 新增自动关卡三星穷举验证脚本。
- 新增进度/存档回归测试。
